= content_tag(:label, dato.fecha ? dato.fecha.strftime("%d-%m-%Y") : nil, :id => "dato_#{dato.id}_fecha", :name => "dato[#{dato.id}][fecha]", :class => "date #{class_editable}", :style => "width: 50px;")